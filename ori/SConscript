Import('env')

src = [
    "main.cc",
    "repo.cc",
    "repo_cmd.cc",
    "scan.cc",
    "util.cc",
]

env.Program("ori", src,
            LIBPATH = [ "$LIBPATH" ],
	    LIBS = [ "crypto", "stdc++", "rt" ],
            CPPPATH = [ "$CPPPATH", "#include", "#" ])

